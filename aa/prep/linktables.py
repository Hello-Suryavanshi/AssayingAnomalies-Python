import pandas as pd


def link_crsp_compustat(crsp: pd.DataFrame, link: pd.DataFrame) -> pd.DataFrame:
    # placeholder: use CCM link table from WRDS (crsp.ccmxpf_linktable)
    # choose primary links, standard link types, and date-valid ranges
    return crsp  # stub for now; implement after first pull of link table
